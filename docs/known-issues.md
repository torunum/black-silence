# Known Issues

Problems found during the port. Phase 0 preserves them deliberately — see the
non-goals in the Phase 0 spec. Each entry names the phase that owns the fix.

| ID | Issue | Owner |
|---|---|---|
| KNOWN-1 | Level 1 places a red key (`K`) and a Guardian miniboss guarding it, but the level contains no locked door (`D`) anywhere. The key is decorative. | Phase 4 — level rebuild |
| KNOWN-2 | `Context` is a service locator, used to make the mechanical port safe: systems reach into each other through its `ctx` object instead of importing each other directly, which would either fail `madge --circular` (two systems calling each other both ways) or reach into code that has not moved yet. Its entry count peaked at six during Plan 0E — `damagePlayer`, `damageEnemy`, `wakeBoss`, `endLevel`, `openPiano`, `showWin` — and Task 10 Step 6 was the first pass that shrank it: once the enemy-AI extraction's four-way split (`ai/Behaviors.ts`/`ai/Locomotion.ts`/`ai/Attacks.ts`/`Boss.ts`) made every remaining cycle-edge look one-way, `damagePlayer`/`damageEnemy`/`wakeBoss` retired in favor of direct imports (from `Player.ts`, `Damage.ts`, `Boss.ts` respectively) — three entries remained, `endLevel`/`openPiano`/`showWin`, all bridges to code that had not been extracted yet rather than cycle-breaks. That `wakeBoss` retirement turned out to be wrong: the `madge --circular src/` run cited as proof was scanning only `src/legacy.js` (madge's default extensions exclude `.ts`), so the four-file cycle the edge actually closes — `Damage.ts -> Boss.ts -> ai/Attacks.ts -> world/Props.ts -> Damage.ts` — went unchecked for five plans (see KNOWN-12). Plan 0F Task 1 fixed the gate (`--extensions ts,js`) and restored `wakeBoss`, back to four, then retired `openPiano` the way the three before it had — moved to `src/ui/Piano.ts`, `src/player/Interact.ts` now importing it directly, confirmed clean — down to three: `endLevel`, `showWin`, `wakeBoss`. Plan 0F Task 2 retired the other two bridges the same way: `endLevel` moved to `src/ui/LevelEnd.ts` (`src/player/Player.ts`'s exit-pad branch now imports it directly) and `showWin` moved there too (`src/enemies/Death.ts`'s `bossDeath` now imports it directly), each confirmed clean one at a time against `madge --circular --extensions ts,js src/` so a cycle would have been attributable to whichever one caused it (neither did). `Context.ts` now holds exactly **one** entry, `wakeBoss` — the locator's only remaining member of the other kind, a genuine cycle-break rather than a bridge to unmoved code, and extracting more code cannot remove it: the four files in the cycle it prevents are all real modules already, so only breaking one of the other three edges would, which is a redesign rather than a port task. **This entry stays open.** The long-term rule (systems talk over `core/Events.ts`, never reach into each other) still holds; each phase after Phase 0 migrates the systems it touches, and `wakeBoss` is a natural first candidate for that bus once it exists — until then the locator holds exactly this one entry, not zero. | Phases 1–5 |
| KNOWN-3 | **Closed by Plan 0F Task 6.** The four gameplay `setTimeout` calls — `WeaponState.ts`'s power-kick hit test and `Behaviors.ts`'s Mancubus second barrel, Slaughtaur second bolt and brute slam damage — were converted by Task 5 to `src/core/Time.ts`'s scaled-time `schedule()`/`tickScheduled()`, so they respect hit-stop and pause. Every remaining `setTimeout` — recounted directly against `HEAD` rather than trusted from this row's own prior count (which had already drifted once): **18** call sites across 12 files (`audio/Ambient.ts` ×1, `audio/Voice.ts` ×1, `enemies/Boss.ts` ×2 — `wakeBoss`'s roar cue and `roarFor`'s own growl, `enemies/Death.ts` ×1 — `bossDeath`'s win-screen delay, `render/Overlay2D.ts` ×1 — the scope blip, `ui/HudMessages.ts` ×2, `ui/Piano.ts` ×1 — the key-highlight clear, `ui/Toasts.ts` ×2 nested, `weapons/WeaponState.ts` ×1 — the shotgun-pump eject, `world/Ambience.ts` ×3, `world/LevelLoader.ts` ×2, `world/RandomEvents.ts` ×1 — a loop that fires three) — now goes through `src/core/Timers.ts`'s new `after(fn,ms)`, a tracked wrapper that calls the same bare `setTimeout` **dynamically inside the function body**, never captured at module scope, so the trace fixtures' fake-clock substitution (installed after modules evaluate) still applies cleanly and neither fixture needed to move. These stay wall-clock on purpose — an audio tail or a UI fade should not stretch out during hit-stop, unlike the four gameplay callbacks above. `src/world/LevelLoader.ts`'s `loadLevel` now calls `clearAllTimers()` and `Time.ts`'s `clearScheduled()` as its first two lines, before any new state is built, so nothing pending from the level being left behind — an audio tail, a UI fade, a scheduled boss attack — can fire into the level replacing it. One of the eighteen is a deliberate behavior change beyond the mechanical swap: `bossDeath`'s `showWin` (THE LIVING HEART's 2.8s win-screen delay) now routes through `after` too, so a level load inside that window suppresses the win screen where a bare `setTimeout` would have shown it over a level the player had already left — the exact bug class this task exists to kill. Proven wired, not just implemented: `tests/integration/timerCancellationWiring.test.ts` registers a timer through the real `after`, calls the real `loadLevel` a second time, and asserts the timer never fires; deleting `loadLevel`'s `clearAllTimers()` line turns exactly that test red, and deleting its `clearScheduled()` line independently turns the file's `Time.schedule` sibling test red — each checked in isolation during this task rather than assumed. | Closed — Plan 0F Task 6 |
| KNOWN-4 | **STILL OPEN — the mechanism is untouched; only level 2's eight pews were taken out of its blast radius (player feedback round 1, task 1, 2026-09-17).** The bug: in `loadLevel` (`src/world/LevelLoader.ts`, the `else if(EDEF[ch])spawnEnemy(...)` / `else if("xTCFVOv".includes(ch))spawnProp(...)` dispatch), the enemy check runs *before* the prop check, and two characters exist in both `ENEMY_DEFS` and the prop set: `C` (Cacodemon enemy vs. "chair" prop) and `V` (THE FACTORY FOREMAN boss — 2600 hp, boss bar, 34 melee — vs. "pew" prop). Enemy wins, so every `C`/`V` tile in any level grid spawns the enemy and never the prop. **That is all still true.** What changed is one level's authoring: `src/world/levels/level2.ts` used to carry 8 `V` tiles — six under a "nave pews + altar + boss + cross launcher" comment, one in the chapel, one in the side aisle — so the church nave held 8 × 2600 = 20,800 hp of Factory Foremen in front of the level's authored boss, the Corrupted Priest (`Q`). The project owner played the game and reported level 2's first boss as much too hard; it was eight of them, and the balance decision this row had been waiting for was made by the person entitled to make it. All eight tiles are now `v`, a **prop-only** spelling of the same pew geometry (`spawnProp`'s `V` branch now also accepts `v`, and `v` is in no other table). Level 2 therefore has no Foreman at all. The chapel and aisle tiles were judged furniture on their own evidence rather than swept along: both rooms already carry their own encounter, both are rooms a church puts benches in, and — decisively — `bossDeath`'s `if(e.key==="V")` arm calls `openExit()`, so *any* Foreman in level 2 opened the exit the Priest's death is supposed to open and let the player skip `Q` entirely. **What remains open, and why this row is not closed:** (1) the dispatch order is unchanged and was deliberately not flipped — `C` is an intentional Cacodemon in levels 6 and 7, so a global reorder would trade this bug for that one; (2) the enemy and prop tables still overlap on `C` and `V`, so the next level that writes a chair or a pew hits this again; (3) **level 2's one `C` tile, the chair in the priest's chambers, is still a Cacodemon** — it was left alone because it is a single ordinary enemy rather than a boss, so it is a level-design call for the rebuild rather than the emergency this row's `V` half was. Levels 6 and 7's `C` tiles are intentional Cacodemons and always were. Pinned by `tests/world/levels.test.ts` (rewritten, not deleted: it now pins `{C:1, V:0}` for level 2, that `v` is claimed by no enemy def, and that no level places an exit-opening boss other than its own) and by `tests/fidelity.test.ts`, which keeps the eight cells as an explicit, enumerated divergence from the frozen reference rather than a hole in the comparison. **This entry covers only the enemy-vs-*prop* collision; there is a second, separate collision class against the *item* map — see KNOWN-11.** Also see `tests/world/levels.test.ts` for a note on `src/world/levels/index.ts`'s grid-legend docstring, inherited from the reference and inaccurate (omits `0`, `7`, `8`, `9`, which do appear in grids). | Phase 4 — level rebuild (the `C` chair, and the table overlap itself) |
| KNOWN-5 | **Closed by Plan 0C Task 1.** `tests/fidelity.test.ts` used to prove a carve was faithful purely by **source-text comparison** — pull a line range out of the frozen `reference/sonsurum.html`, pull the matching function body out of the ported module, normalize away TS-only syntax, assert the two strings are equal. That only works while ported code moves verbatim, which a later global-to-state migration cannot do by definition. The fix, exactly as recommended below: a **behavioral recorder**. `tests/support/recordingCanvas.ts` is a Proxy-based 2D context stub that logs every method call and every property assignment, in order, instead of no-op'ing them — including a stable, creation-ordered id for gradient objects (`Gradient#1`, ...) wherever one is used as a logged value (e.g. `fillStyle = someGradient`), so a bug that assigns the wrong (but still previously-created) gradient is a visible diff, not two indistinguishable empty Proxies — and a `getContext` boundary entry per canvas, tagged with a creation-ordered canvas id and that canvas's width/height, so canvas *reuse* and a wrong dimension are visible too (the 2D-context recorder alone has no way to see either: reusing one canvas across many textures doesn't change the sequence or arguments of any single draw call, only where they all end up rendered, and width/height are set on the canvas *element*, which the 2D-context recorder never wraps). `tests/support/recordingAudio.ts` is the WebAudio equivalent, logging node creation, connect edges, parameter assignments/automation, and start/stop, with every created node **and every AudioParam** tagged a stable creation-ordered id so two independent sessions building the same graph produce deep-equal logs despite being different objects (an early version only tagged nodes; `audioInit` connects an LFO straight into another oscillator's gain param, which leaked the live param object into the log before this was caught). Both sides of every comparison — the reference chunk (run via `evalReference`'s `vm` sandbox) and the ported module — draw against the same recorder and the logs are asserted equal via `tests/support/expectCallLogEqual.ts`, which finds the first divergent index with a cheap linear scan before falling through to a real `toEqual` on the equal path — a real mismatch on `buildTextures`' ~30,000-entry log made Vitest's default diff printer take on the order of a minute, which discourages re-running the suite on a genuine regression. Because the drawing/synthesis code calls `Math.random()` directly (`noiseFill`) or via `src/utils/math.ts`'s `rnd`/`pick`, `tests/support/seededRandom.ts` installs a deterministic PRNG over the global `Math.random` for the duration of each comparison — critically, the *outer* `Math` object (the one being patched) must be passed into `evalReference`'s sandbox globals, because a `vm` context otherwise gets its own separate realm intrinsics and would silently ignore the seed. The new tests live in `tests/behavior/textures.test.ts` (`buildTextures`; `texFromPx` on a representative sprite across plain, mirrored, headless/`blankTop`, and masked+stumped dismemberment variants; `buildItemTex`) and `tests/behavior/audio.test.ts` (`audioInit`; `blip`/`bang`/`boom`/`growl` each including their default-argument fallback branch; `snarl` across all eleven archetype branches, not a sample); each was sabotage-tested (a colour, a call-order swap, a loop bound, a frequency, a `connect` target, a gradient swap, a canvas-reuse bug, a canvas-dimension bug) and confirmed to fail for the right reason before being trusted — a first review round caught three real blind spots (`blankTop` uncovered, `snarl` only 4/11 archetypes covered, canvas reuse/dimensions invisible) and two latent ones (gradient identity, an audio-session restore that skipped `finally`) by mutating real source and watching the suite stay green; all five are fixed and re-proven above. The now-redundant byte-identity tests for those specific functions were removed from `tests/fidelity.test.ts`; every data comparison (`PXDEF`, `ENEMY_DEFS`, `WEAPON_STATS`, `MONOLOGUE`, the level grids) and every byte-identity test for a function the recorder does *not* exercise (`buildSprites`, `click`, `noiseBuf`, `gurgle`, `pain`, `deathCry`, `wetDoor`, `stoneDoor`, `bellToll`, `organChord`, `pianoNote`, `startBossMusic`, `stopBossMusic`) were deliberately kept. **What the recorder still cannot reach**, stated plainly rather than claimed away: (1) `buildSprites`' own dismemberment region arithmetic (`armTop`, `armBot`, the mask rectangles) decides *what* to pass to `texFromPx` but issues no draw call itself, so a recorder watching `texFromPx` can't see a regression in that math — it stays guarded by a normalized-source-text test. (2) The eleven Voice/Ambient/Sfx functions listed above have no recorder at all, only the original byte-identity guard — a future task extending `tests/behavior/audio.test.ts` to them would let those retire too. (3) Raw per-sample buffer fills (`noiseBuf`/`bang`/`boom`'s `d[i]=Math.random()*2-1` loops) are plain typed-array writes, not method calls, so the recorder is blind to a change in the *fill formula* itself — only to a change in the buffer's create-call arguments (length, sample rate). (4) Beyond `snarl` (now exhaustive across all eleven branches) and the default-argument branches called out above, the other functions still only cover the specific call/argument combinations each test chose (`blip`'s square-vs-sine/lowpass branch, `bang`'s highpass-vs-not, `texFromPx`'s mirror/masks/blankTop/stumps variants) — not exhaustive over every possible argument. (5) As always, this is a call-log comparison, not a pixel or audio-sample comparison — it proves the same sequence of drawing/synthesis operations happened with the same arguments, not that the rendered pixels or audible sound are perceptually anything in particular (no oracle in this suite has ever verified that). | Closed — Plan 0C Task 1 |
| KNOWN-6 | **Closed by `tests/behavior/overlay2d.test.ts` (26 cases, Plan 0C).** The fix was a test-writing job with no source changes, as predicted. `sizeFx`, `ejectCasing`, `screenBlood`, `spawnPuff` and `fxTick` are now compared call-for-call against the reference's own originals (`REF.overlay2d` plus a new `REF.fxTick` range, carved out of `REF.viewmodelDraw` so the injected `drawKickBoot`/`drawViewmodel` stubs are not shadowed by the reference's own declarations). Re-running all seven of the sabotages below now fails the suite. Five more sabotages chosen independently of the original seven (scope radius `.42`, scope threshold `.5`, kind-3 casing colour, puff growth `8`, and `Math.round`→`floor`/`ceil` in `sizeFx`) are all caught; the rounding one was caught only after the viewport list gained two aspect ratios where `round`, `floor` and `ceil` disagree — the first version of the test had five viewports that all divided evenly. **`life:1.6`'s exception is closed** (Phase 2 Part A Task 2, alongside KNOWN-7 below): it used to be unobservable because the casing was always culled by `c.y>VH+10` first (max airtime ≈0.71s at any ordinary aspect ratio); once KNOWN-7's fix let a casing survive at all, the same offscreen cull still wins at every *ordinary* aspect ratio (survival is now ≈0.94-1.15s there, still under 1.6s) — so the pin needed a viewport tall enough (`TALL_VIEWPORT`, `VH=4800`) that the offscreen cull can never fire within 1.6s, letting life expiry be the thing that removes it. `1.6`→`1.9` now fails a named test (`"removes the casing when its life (1.6s) runs out…"`). *Original entry:* **`src/render/Overlay2D.ts` has zero test coverage.** `fxTick`, `sizeFx`, `ejectCasing` and `screenBlood` — the shell-casing arc, the screen-blood flash, the sniper-scope vignette and the overlay's resize math — are exercised by nothing. Proven during Plan 0C Task 3's review by applying seven simultaneous art/physics changes (casing gravity `240`→`260`, blood fade `.6`→`.7`, `FW` `640`→`648`, casing eject height `FH*.62`→`FH*.66`, casing life `1.6`→`1.9`, blood splat count `5`→`6`, blood radius `rnd(6,22)`→`rnd(6,26)`) and watching all 272 tests pass. No coverage was *lost* — this code was untested before it was carved out — so it is a gap, not a regression, and the moved bodies were verified verbatim against the reference by eye. Two things make it worth closing before the global-to-state migration: `fxTick`'s signature changed during that carve (it now takes `zoomLerp` plus two callbacks, to break an `Overlay2D`↔`draw.ts` import cycle), making it the highest-risk code in that diff; and later plans will reasonably assume these ~114 lines are guarded. Evidence it was overlooked rather than judged: `REF.overlay2d` was added to `tests/support/reference.ts` and is consumed by zero tests — scaffolding for a comparison never written — so closing this is a test-writing job with no source changes. Beware the two traps this project has already hit: a test that passes a zeroing parameter (a gib test used `pow=0`, flattening the very literals it should have probed), and coverage that is present but not real (the recorder once passed while 20 textures were collapsed onto a single canvas). | **closed** |
| KNOWN-7 | **Closed by Phase 2 Part A Task 2.** `ejectCasing` now spawns a casing at `x:VW/2+rnd(4,12), y:VH*.62` and `screenBlood` now spawns splats at `rnd(0,VW), rnd(0,VH)` — the same `VW`/`VH` (320-wide) space `fxTick` actually draws and culls in, instead of the upscaled `FW`/`FH` (640-wide) space. Velocities, gravity, `life`, radius and the per-`kind` art were left exactly as authored — only the two spawn expressions changed. Casings now survive and draw at every ordinary aspect ratio (pinned by `tests/behavior/overlay2d.test.ts`'s four-viewport case, inverted from the test that used to prove they never did), and `WIDE_VIEWPORT`/8000×1000 — previously needed just to reach the per-`kind` art at all — is gone from the suite entirely, which is itself evidence the fix holds at real aspect ratios. `reference/sonsurum.html` is never edited, so it still has the original bug; the affected tests (`screenBlood` and `ejectCasing` describes) assert the module's fixed behavior and the frozen reference's unchanged behavior side by side rather than through a call-log parity check, each commented as the deliberate divergence. **The motion tuning itself (spawn velocities, gravity, life) has still never been seen by a human** — this task deliberately did not touch it, because this environment cannot render the game (`requestAnimationFrame` never fires here, see the Phase 2 Part A plan's Global Constraints) and a visual judgement needs a human watching it. Phase 2 Part B, when someone can actually look at the game running, may want to revisit whether the casing arc (gone in well under 1.2s at ordinary aspect ratios) and the blood-flash spread read well on screen. *Original entry:* **Spent shell casings never reach the screen, and most screen-blood splats land off-canvas.** `fxTick` scales the overlay context by `SF=FW/320` and then draws everything in 320-space, but `ejectCasing` spawned a casing at `x:FW/2+rnd(4,12), y:FH*.62` — coordinates in the *upscaled* `FW`/`FH` space. Since `FH=640/a` and `VH=320/a`, the spawn point `.62*640/a` cleared the cull line `VH+10 = 320/a+10` for every aspect ratio below `a=7.68`, so the casing was spliced away on the very first tick, before it was ever drawn. Every real window is far below 7.68 (16:9 is 1.78), which meant the entire casing draw path — the four `kind` colours, the brass stripe on the shotgun shell, the spin, the gravity arc — was unreachable in the shipped game. `screenBlood` had the same coordinate-space mix-up without the cull: its splats were placed at `rnd(0,FW), rnd(0,FH)` and drawn in 320-space, so only the roughly one-quarter of them that landed in the top-left `VW`×`VH` corner were visible; the rest were drawn off-canvas every frame until they faded. Found while closing KNOWN-6, not before: it is exactly the kind of bug a source-text fidelity check cannot see, since the port copied both functions verbatim and was faithful. | **closed** |
| KNOWN-8 | **Closed by `tests/behavior/input.test.ts` (Phase 2 Part A Task 1).** `src/player/Input.ts`'s wheel handler now scans `i=(i+(e.deltaY>0?1:7))%8` for eight iterations, cycling all eight slots in slot order — matching what the keyboard's `/^Digit[1-8]$/` already did. The backward step is `n-1` (7, not a leftover `5`), which matters: at `%8`, a step of `5` still visits every slot (`5 ≡ -3 mod 8`, so it walks backward three slots at a time), so a test that only checks reachability would not catch it — that is why `"wheel-up from slot 0 lands on slot 7, the modular predecessor, not slot 5"` exists as its own assertion, pinned by cur=0. This is a **deliberate Phase 2 divergence from the reference**, which is never edited and still scans six: `tests/behavior/input.test.ts`'s wheel-affected cases no longer run through `expectParity` (which asserts the two sides agree) but assert the module's new eight-slot behavior and the reference's unchanged six-slot behavior side by side, each with a comment naming the divergence, so the reference stays in the loop as a record of what changed rather than being silently dropped. The row's own caveat still holds in the new form: the loop bound is not independently testable in general, only the modulus — eight iterations of a mod-8 walk already visit every slot, so a longer loop only revisits slots already rejected. It *is* observable in one specific shape, which `"reaches the last two weapons with the wheel now that it cycles all eight slots"` uses deliberately: starting at slot 7 and scrolling forward to slot 6 needs the full seven hops available under mod 8, so a loop still bounded at 6 stops one hop short and never finds it. *Original entry:* **The mouse wheel can only reach six of the eight weapons.** The wheel handler scanned `i=(i+(e.deltaY>0?1:5))%6` for six iterations, so it cycled slots 0-5 and stopped — while `S.weapons`, `S.mag` and `WEAPON_STATS` all have eight entries, and the keyboard's `/^Digit[1-8]$/` reaches all eight. The NAIL CANNON (slot 6) and the SOUL REAPER (slot 7) were therefore switchable only with `7` and `8`; a player who used the wheel would never find them. The cause is visible in the reference's own section banner, which still reads "WEAPONS — 6 slots" above an eight-slot table: the roster grew and the wheel did not. Found while closing out Plan 0C Task 5, and pinned by `tests/behavior/input.test.ts` — owning *only* slots 6 and 7 made the wheel do nothing at all while `Digit7`/`Digit8` still worked. | **closed** |
| KNOWN-9 | **Closed by `tests/integration/wiring.test.ts` (16 cases) and a woodP parity case in `tests/behavior/fx.test.ts`.** The wiring test boots the real `legacy.js` under jsdom, starts a real level and runs real frames, with only the three modules under test swapped for recorders. Two techniques reach the seams: **sentinel accessors** (`Input.ts` is mocked so `getSwayX()`/`getSwayY()` return distinct recognisable numbers, making a transposition a failed equality instead of two indistinguishable floats) and **cross-checks between independent readers of one global** (the hooks and the frame both report `S.cur`; the player always owns the weapon they hold). All three review sabotages now fail, plus eight more chosen independently — wrong canvas, `isStarted` reporting `pianoOpen`, `ownsWeapon` always true, `doKick` wired to `startReload`, a dropped frame field, and `drawViewmodel` receiving `t` as `dt`. woodP is now compared against `REF.woodP` buffer-for-buffer through launch, ground contact and expiry, which pins its velocities, all three colour channels, its life and its `kind`; ten sabotages caught. **Two residual seams are documented rather than closed**, both the same shape — a value indistinguishable from what it could be confused with in every reachable state: `dead:S.dead` vs `S.won` (both false for a living player; dying or winning is not reachable in a two-frame prologue) and `zoomLerp` vs a literal `0` (it only rises with the sniper scoped, and the prologue loadout is slot 0 only). Enabling the domStubs to run a real frame needed three WebGL log methods, an `ACTIVE_UNIFORMS`/`ACTIVE_ATTRIBUTES` count of 0, and a full AudioParam scheduling surface. *Original entry:* **The wiring between `legacy.js` and the extracted modules has no test coverage.** Every parameterized carve moved state-*reading* out of the extracted function and into a call site in `src/legacy.js`, and no test exercises those call sites. Two seams matter most: the 18-field `ViewmodelFrame` literal plus `kickAnim` and `zoomLerp` at the `fxTick(...)` call (`src/legacy.js:1624`), and the 12-entry `setInputHooks({...})` object (`src/legacy.js:80`). Proven by sabotage during the Plan 0C whole-branch review: `swayX:getSwayY()`, `drawKickBoot(0)` and `currentWeapon:()=>S.cur+1` each leave **all 331 tests green** while breaking the viewmodel sway, the kick-boot animation and the sniper-zoom/wheel weapon slot respectively. The module-level oracles cannot see any of it by construction — `tests/behavior/viewmodel.test.ts` builds its own frames and `tests/behavior/input.test.ts` installs its own hooks, which is exactly what makes them good tests of the *modules*. Both mappings were verified correct field-by-field against every bare global the reference's `drawViewmodel`/`drawKickBoot`/INPUT section reads, so this is a coverage gap and not a live defect. A third, smaller instance: `woodP`'s velocity, size, life and `kind` arguments are unpinned (only its count and colour channel are asserted), and `REF.woodP` is the one range in `tests/support/reference.ts` that no test consumes — the same "unused scaffolding over uncovered code" signal that preceded KNOWN-6. | **closed** |
| KNOWN-10 | **Closed by `tests/integration/combatTrace.test.ts` (7 cases, Plan 0E Task 1).** A second `runTrace` fixture, played against level 1 (15 enemies) instead of the prologue, with a script that walks the player through the level's one corridor+door and then spins in place firing — a straight-ahead walking-and-firing script, the shape `trace.test.ts` uses, never lands a hit here, because the pistol's hitscan needs the shot within roughly half an enemy's width of dead-center. Proven by sabotage to catch: the armour absorb fraction in `damagePlayer` (needs `S.armor` set nonzero in the test setup first — level 1's grid cannot place an armour pickup at all, `EDEF["A"]` shadows the item letter, so this sabotage passed unnoticed on the first attempt with `S.armor` at its default 0), a constant damage-scale factor in `damageEnemy`, a 10% enemy move-speed change in `enemyTick`, and an inverted return in `los`. A fifth sabotage — skipping `killEnemy`'s `S.kills++` — has no effect on camera, scene, or any HUD field the trace records, so it is asserted directly against the live `S.kills` from `src/core/State` rather than through the fixture; this is the one case where the task's "widen what the trace records" instruction was actually needed. `hitStop` and `spawnGuard` remain covered directly via their exported state properties in `tests/integration/wiring.test.ts`, as before. *Original entry:* **The combat-resolution path has no automated coverage.** `tests/integration/trace.test.ts` is the branch's characterization net, but it plays the prologue, which loads with **zero enemies** (`world.enemies.length === 0` right after load, confirmed live in a browser and corroborated by the committed fixture: `hp` is `HEALTH100` in all 90 recorded frames, no enemy-sighting bark ever fires, and `scene.count` rises monotonically 237→249 with no frame where anything is removed). Nothing the script fires at can be hit and nothing can hit back. So `damagePlayer`, `damageEnemy`, `killEnemy`, `severLimb`, `spawnHead`/`headTick`, `crossExplode`, `explodeBarrel`, `projTick`'s hit branches, `ringTick`/`strikeTick`/`poisonTick`, `bossDeath`, `wakeBoss`/`cineTick`, `endLevel` and `showWin` are all unexercised — every one of them was rewritten during Plan 0D's migration. Residual risk is low (the rewrites are mechanical, and Task 8b's set-comparison proved the only textual change was the state-object prefix), but it is not zero, and three constants are provably invisible as a result: `hitStop`'s `dt*=.08` and `spawnGuard`'s `2.0` — both since given direct tests via their now-exported state properties in `tests/integration/wiring.test.ts` — and `S.kills` vs `S.gibs` in the level-end score, which only `endLevel` reaches. Closing this needs a trace against a level that has enemies, or focused unit tests over the damage functions; it should not be closed by loosening the existing fixture. | **closed** |
| KNOWN-11 | **Armour pickups never spawn from a level grid, and 20 extra Mancubus spawn in their place.** `loadLevel`'s dispatch is `else if(EDEF[ch])spawnEnemy(...)` / `else if("xTCFVOv".includes(ch))spawnProp(...)` / `else { const map2={h:"health",A:"armor",…}; }` — the enemy check runs first, and `A` is both a Mancubus in `ENEMY_DEFS` (260 hp, twin barrels) and the armour key in `map2`. So `map2`'s `A:"armor"` entry is unreachable dead code, and every `A` tile spawns a 260 hp Mancubus instead of a `+50 ARMOR` pickup. Counted across the grids: 1 in level 1, 2 in level 2, 3 each in levels 3-5, 4 each in levels 6-7 — **20 tiles**. The consequence is that the only armour the player can obtain in the entire game is the challenge-plate reward, which reaches `world.items` through a different code path; `S.armor` is otherwise 0 for every run. Identical in `reference/sonsurum.html` (same dispatch order at line 2935, same `map2` at 2938), so this is faithful port behavior, not drift. **This is the same structural bug as KNOWN-4 but a different collision class** — KNOWN-4 is enemy-vs-prop (`C`, `V`), this is enemy-vs-item (`A`), and fixing either means deciding what the level author actually intended, which is a level-design call. Found while building Plan 0E's combat trace: an armour-absorption sabotage passed undetected because `S.armor` is structurally always 0, so that test now seeds it explicitly. | Phase 4 — level rebuild |
| KNOWN-12 | **`madge --circular src/` scanned exactly one file for five plans, and a real import cycle survived because of it.** The `npm test` script has run `madge --circular src/` since Plan 0A. madge's default extension list is `js,jsx`; it excludes `.ts`. So the command scanned only `src/legacy.js` and printed "✔ No circular dependency found!" having never looked at the 76 TypeScript modules the port was built out of. The "Processed 1 file" line immediately above that success line said so every single run, and it went unread in every task report across Plans 0C, 0D and 0E — including by the agent writing the reports. **The consequence was not hypothetical:** Plan 0E Task 10 (399763e) retired the `ctx.wakeBoss` locator entry in favour of a direct `Damage.ts -> Boss.ts` import and cited a clean madge run as proof. That import closes `Damage.ts -> Boss.ts -> ai/Attacks.ts -> world/Props.ts -> Damage.ts`. The reasoning at the time checked the one edge by eye and found it one-way, which it is — the cycle closes transitively, three files away, which is exactly the class of thing the tool exists to catch. Fixed in Plan 0F Task 1 (0e1722b): the script now passes `--extensions ts,js`, the entry is restored and registered by `Boss.ts`, and `tests/integration/contextWiring.test.ts` pins it against two sabotages. **The general lesson is about verification theatre, not madge:** a gate that reports success while examining nothing is worse than no gate, because it is quoted as evidence. When a tool prints how much work it did, read that number. | **closed** |
| KNOWN-13 | **Closed by Phase 3 Part A (branch `phase-3a-one-enemy-shape`).** There is now one `src/enemies/Enemy.ts`, and it is derived from the **producer**, field by field: `spawnEnemy`'s object literal in `src/world/LevelLoader.ts`. That direction is the whole fix. The root cause was never the consumers — `spawnEnemy` returned `Record<string, unknown>`, so there was nothing for any of them to disagree *with*; an interface the producer does not have to satisfy would have been just a seventeenth unchecked declaration. So `spawnEnemy` now **returns `Enemy`**, `world.enemies` is **`Enemy[]`** (and `world.bossRef` is `Enemy | null`), and all sixteen local shapes became `Pick<Enemy, …>` aliases. The fifteen `as unknown as SomeEnemy[]` casts at the array's use sites are gone, replaced by `readonly SomePick[]` local bindings — a *checked* widening, which the compiler verifies against the real `Enemy`, where the cast verified nothing. The `Pick<>`s themselves deliberately stay: the per-file "declare only what this code path reads" discipline this row called defensible is still worth keeping, and it is now enforced rather than asserted. `Enemy` has 67 non-optional fields (exactly what the literal builds), 6 optional post-spawn fields, and 10 optional fields that are never written at all — which is KNOWN-15 below, found *by* this consolidation. **The one outright contradiction is resolved**: `severKey` was `string` in `src/enemies/Damage.ts` (which assigns it) and `boolean` in `src/enemies/ai/Behaviors.ts` (which reads it); it is now the union `"noLegs"\|"noLArm"\|"noRArm"\|"gibbed"`, exactly what `refreshSeverSprite` assigns, guarded by a two-directional `Exactly<A,B>` type check so neither a narrow-to-`boolean` nor a widen-to-`string` mutation survives. Both directions are pinned at runtime too, by `tests/enemies/enemyShape.test.ts`, which parses `Enemy.ts` as text and compares its non-optional names to the own-keys of a real spawned enemy — no count is hardcoded anywhere, so nothing here can go stale. **The count in this row's own title was wrong, twice, in opposite directions**, which is worth recording as its own finding: the row said *fifteen* when there were already **sixteen** (`MusicBossEnemy` arrived with Phase 1's adaptive music, after this row was written), and the Phase 3A plan's audit then said *seventeen*, because the scanner swept in `EnemyDefs.ts`'s `EnemyDef` — the static def table, a genuinely different shape, and the very one the audit had excluded by name. The true figure is **sixteen interfaces across fifteen files**, measured with `git grep` at `db5b390`. That is the fourth time in this project a number nobody re-derived was quoted as evidence (see KNOWN-12 for the most expensive one), and it is why every count in the new tests is derived at runtime instead of written down. *Original entry:* **Fifteen separate interfaces describe the same enemy object.** `AttackEnemy`, `MoveEnemy`, `BossBrainEnemy`, `DamageEnemy`, `KillEnemy`, `DeathEnemy`, `TickEnemy`, `BossEnemy`, `HitscanEnemy`, `KickEnemy`, `DamageableEnemy` and four files each declaring a plain `Enemy` (`ai/Behaviors.ts`, `ai/Perception.ts`, `fx/ProjectileTick.ts`, `world/RandomEvents.ts`) are all local cast shapes over the same runtime object — the untyped record `spawnEnemy` pushes into `world.enemies`. The convention was deliberate and defensible while the port was in motion: each file declares only the fields its own code path reads, so a file cannot silently depend on a field it never checks. Six existed before Plan 0F Task 8; enabling `noImplicitAny` needed five more and surfaced four pre-existing ones, which is what made the scale visible. **The risk is drift, not correctness today**: nothing keeps these fifteen agreeing about a field's type, and a change to the real spawned shape updates none of them — the same duplication class Plan 0E Task 3's review flagged for `Seg`/`CollProp` and Task 6 fixed by exporting `Prop` from `src/world/Props.ts` instead of redeclaring it. The fix is one shared `src/enemies/Enemy.ts` (the spec's §5 layout already lists that file) with optional fields, which every one of these becomes an alias of or a `Pick<>` from. It was deliberately NOT done in Task 8: consolidating a shape across fifteen files is a cross-cutting refactor deserving its own review, and bundling it into 'turn on a compiler flag' is how a mechanical task becomes a risky one. | **closed** — Phase 3 Part A |
| KNOWN-14 | **Three.js is pinned at 0.128.0 (2021) and the upgrade is deferred to Phase 2.** Plan 0F Task 11's deliverable was a decision backed by evidence, not necessarily an upgrade, and the evidence says defer. Current latest is **0.185.1** — 57 minor versions ahead. The port uses 34 distinct `THREE.*` APIs and every geometry, material and light class among them still exists, so the upgrade does not fail to *compile*; it fails to be *verifiable*. Three specific breakages: (1) `src/render/RenderCore.ts:47` reads `if (THREE.sRGBEncoding !== undefined) renderState.renderer.outputEncoding = THREE.sRGBEncoding;`. On r128 `sRGBEncoding` is `3001` and the line runs; on any modern three it is `undefined` (renamed `SRGBColorSpace`, with `outputEncoding` renamed `outputColorSpace`), so the guard makes the line a **silent no-op** and sRGB output encoding quietly switches off. (2) Colour management became enabled by default in r152, which shifts every colour again. (3) Light intensities changed meaning in r155 (`useLegacyLights` defaulting off), and this game's look is hand-tuned point lights. **Why this cannot be settled here:** the characterization traces pin the camera, a scene-graph digest and HUD fields — they do not sample pixels, and `tests/behavior/` compares canvas and WebAudio *call logs*, not rendered output. A colour-management change is invisible to every test in this repository, so the suite would stay green while the game looked different. **That last sentence was wrong, and was corrected on 2026-09-14:** the game *does* render in the browser pane, and Phase 2B Part A ran the side-by-side it asks for — two dev servers, one per revision, matched frames from each. What the comparison showed: r186 is dramatically darker, the torch-lit walls of the prologue and of level 1 going nearly black, exactly as the branch's reading of the shader source predicted (every diffuse contribution pi times smaller, plus a changed falloff *shape*). The branch `phase-2b-threejs-evaluated` is not merged; the remaining decision is which reversal to take, and it is the project owner's. See `docs/STATUS.md`'s Environment gotchas for what the pane can and cannot do. **A green suite is not evidence for this particular change, and claiming it would be the most damaging thing this task could produce.** Upgrading needs a human running the game side by side with `reference/sonsurum.html`. The Phase 0 spec §9 named this the highest risk in the phase and pre-authorised exactly this deferral, to Phase 2, where lighting is being reworked anyway and a deliberate art change is in scope. | Phase 2 — combat feel and lighting |
| KNOWN-15 | **Ten `ENEMY_DEFS` fields are authored, are read, and never reach the enemy.** `spawnEnemy` (`src/world/LevelLoader.ts`) builds its enemy with an explicit object literal — no spread — and ten fields that `src/enemies/EnemyDefs.ts` authors are simply not in it. Nothing in `src/` writes them afterwards either (proven by a source scan in the test below, not asserted). So every read of them sees `undefined`, on every enemy, always. Derived from `ENEMY_DEFS` at `5c7f403`, with the enemy keys that author each: **`orb` 7** (`C,A,j,k,q,R,y`), **`fly` 5** (`C,L,q,R,y`), **`flyH` 5** (same five), **`fling` 4** (`z,t,n,G`), **`sovereign` 5** (`Z,N,H,V,G`), **`twin` 1** (`A`), **`burst` 1** (`q`), **`charger` 1** (`L`), **`shield` 1** (`k`), **`title` 12** (8 boss `E,U,Q,Z,N,H,V,G` + 4 non-boss `k,q,R,y`). **What the player therefore never sees:** no enemy fires the projectile its `orb` names — `fireOrb` is perfectly reachable (it is gated by `range`, which *is* copied), but every one of its seven orb-specific colour/damage/speed branches falls through to the default purple 15-damage bolt, so seven enemies that name seven different projectiles all shoot the same one; the five flying enemies do not fly (their muzzle and sprite sit at ground height instead of their authored hover); the Mancubus never fires its second barrel, the Afrit never spreads, the Lost Soul never charges; the Ghoul — the commonest enemy in the game — the Thrall, the Ettin and THE LIVING HEART never throw flesh; the Slaughtaur's directional shield absorbs nothing; and the five sovereign bosses use the non-sovereign stat block. `title` is the tenth and the only harmless one: its sole read (`src/enemies/Boss.ts:121`) is `e.title||EDEF[e.key].title`, and that `||` fallback is the entire reason the boss title bar shows anything — the four non-boss titles are dead data even so, since that read site is boss-only. **`reference/sonsurum.html` does exactly the same thing**, so this is faithful port behavior and not drift; it was found by Phase 3 Part A's consolidation of the enemy shape (KNOWN-13), which is what made "the producer builds these and not those" visible for the first time. **Pinned, not fixed, by `tests/enemies/deadDefFields.test.ts`**, three ways: the defs really do author each field (a guard, so the loop below cannot be vacuous); every authoring enemy spawns without it, checked with `in` rather than `=== undefined` so the field is absent and not merely unset; and, at the read site, a real Cacodemon's `fireOrb` produces the default `0x9a4ae0`/15 bolt from a ground-height muzzle instead of its authored `caco` green plasma from 1.7 units up. Every count is derived from `ENEMY_DEFS` at run time and every authoring enemy is spawned and checked, so nothing in that file can go stale — deliberately, because three separate written counts for these very fields were wrong while this row was being drafted. Four mutations were run against it and each turned a named test red: making `spawnEnemy` copy `orb` (which also reddens two `enemyShape.test.ts` cases), writing `e.fly=true` inside `enemyTick`, and deleting `Boss.ts`'s `||EDEF[e.key].title` fallback. **Do not "fix" this by making `spawnEnemy` copy them.** Turning ten behaviors on at once is a combat-balance change — five enemies start flying, seven start firing different projectiles, five bosses get different stats, all in the same commit — in a game this environment cannot render (`requestAnimationFrame` never fires in the browser pane; see `docs/STATUS.md`'s Environment gotchas). It needs a human at the game, one field at a time, and it belongs to the roster work. **See KNOWN-18: `burst`'s only author is `q`, and `q` is itself unreachable — placed in no level grid, summoned nowhere by name — so making `spawnEnemy` copy `burst` would still leave it dead, because no enemy carrying it is ever spawned.** See `docs/superpowers/specs/2026-09-12-phase3-roster.md` §7 for which of these ten the new roster actually turns on, field by field, with the balance consequence of each. | Phase 3 — enemy roster |
| KNOWN-16 | **First half closed by Phase 3 Part B Task 3; second half pinned, deliberately not fixed.** (1) **Closed.** `addBlob` (`src/render/RenderCore.ts`) now returns `THREE.Mesh<THREE.PlaneGeometry, THREE.MeshBasicMaterial>` — the precise type, matching what it always built — and `Enemy.blob` (`src/enemies/Enemy.ts`) carries that same precise type instead of bare `THREE.Mesh`. The cast this bought back at `src/enemies/ai/Behaviors.ts` (`(e.blob.material as THREE.MeshBasicMaterial).opacity`) is gone; the line now reads `e.blob.material.opacity=.3` with no cast. Both of `addBlob`'s other call sites (`src/world/LevelLoader.ts`'s `spawnEnemy` and `spawnProp`) were checked and neither cast its return value, so there was nothing else to clean up. Proven: regressing the return type back to bare `THREE.Mesh` makes `tsc --noEmit` fail at the `Enemy.blob` assignment in `LevelLoader.ts` (`spawnEnemy`) with "Property 'parameters' is missing in type 'BufferGeometry' but required in type 'PlaneGeometry'" — confirmed, then reverted. (2) **Still open, on purpose.** `EnemyDef.deathBoom` is authored on exactly one enemy (`q`, the Afrit) and read **nowhere** in `src/` — not from the def, not from a spawned enemy. It is a dead *def* field of a different class from KNOWN-15's ten: those are read and never delivered, this one is never read at all. The Afrit's death explosion does happen — `src/enemies/Death.ts` hardcodes `if(e.key==="q")` instead of consulting the flag — so the behavior is not missing, only the data-driven route to it. Deleting the field or wiring `killEnemy` to it are both defensible; both belong with the roster work that owns the def table, so neither was done here. It is now **pinned** instead, the same way KNOWN-15's ten are: `tests/enemies/deathBoom.test.ts` asserts (a) `deathBoom` is authored on exactly one `ENEMY_DEFS` entry (derived from `ENEMY_DEFS` at run time, never hardcoded) and (b) it is referenced nowhere in `src/` except that one authoring line and its `EnemyDef` declaration. That second check deliberately does **not** reuse `deadDefFields.test.ts`'s write-scan regex (`\.field\s*=(?![=>])`) — that regex is narrow by design because KNOWN-15's ten fields have many legitimate reads it must not flag, and narrowing to dot-notation-assignment is a documented, acceptable blind spot *for a write scan* on fields nothing writes via bracket access or compound assignment anyway. A *read* scan pinning "read by nothing" has no such legitimate-reads-to-exclude, so anchoring it to any particular access syntax would just reintroduce that blind spot for no benefit; `deathBoom.test.ts` instead matches the bare identifier `deathBoom` anywhere in `src/` (comments stripped), which catches dot access, bracket access, destructuring and compound assignment alike. Proven by two mutations, each caught by name: authoring `deathBoom` on a second def (`z`) turns red both "guard: is authored on exactly one ENEMY_DEFS entry" and "is referenced nowhere in src/ except its declaration and its one authoring line"; adding a bracket-notation read in `Death.ts` (`(e as unknown as Record<string, unknown>)["deathBoom"]`, the exact form a dot-notation-only regex would miss) turns red "is referenced nowhere in src/ except its declaration and its one authoring line" alone. Both mutations were reverted after confirming. **See KNOWN-18: `deathBoom`'s only author is also `q`, and `q` is itself unreachable — placed in no level grid, summoned nowhere by name — so wiring `killEnemy` to consult it would still do nothing, because no enemy carrying it is ever spawned.** See `docs/superpowers/specs/2026-09-12-phase3-roster.md` §7.1: the roster cut deletes `q`, which deletes `deathBoom` with it — the cheapest close available, to be taken deliberately rather than as a side effect. | Phase 3 — enemy roster (deathBoom half only; the addBlob half is closed) |
| KNOWN-17 | **Closed by Phase 3 Part B Task 3.** `tests/integration/musicCancellationWiring.test.ts`'s three `as unknown as typeof X` casts are gone, the same way `tests/enemies/walkFrames.test.ts` closed the identical shape last plan: the real exports turned out to be structurally assignable to the file's own narrow local shapes once tried without the cast. `S` (`{ dead; won; hp; armor }`) and `player` (`{ spawnGuard }`) needed no other change — `await import(...)` destructures straight onto them now. `world` needed one adjustment, not a cast: its local shape's `enemies: Array<Record<string, unknown>>` does not typecheck against the real `Enemy[]` (`Record<string, unknown>` requires an index signature that `Enemy` doesn't have — `tsc` names exactly this: "Index signature is missing in type 'Enemy'"), so it is now `Array<Pick<Enemy, "boss" \| "dead" \| "dormant">>` — the same "declare only the fields this file's own literals and reads need" discipline KNOWN-13 established, and exactly the three fields `src/audio/Music.ts`'s own `MusicBossEnemy` (`Pick<Enemy, "boss" \| "dead" \| "dormant">`) already reads off this same `world.enemies`. Proven: retyping `GameState.armor` from `number` to `string` in `src/core/State.ts` makes `tsc --noEmit` fail at this file's `({ S } = await import(...))` line ("Type 'GameState' is not assignable to type '{ dead: boolean; won: boolean; hp: number; armor: number; }'"); retyping `Enemy.dead` from `boolean` to `string` makes it fail at this file's `world.enemies = [{ boss: true, dead: false, dormant: false }]` fixture line. Both mutations were reverted after confirming, and both are exactly the class of retype this row warned would previously pass silently. | **closed** |
| KNOWN-18 | **Two of the twenty-five `ENEMY_DEFS` letters can never reach the player: `B` and `q`.** Found while measuring how coupled the coming roster cut (25 types down to 9 plus 3 bosses) is to the level grids. `tests/world/rosterReach.test.ts` derives, by *building* every level module (never by scanning `.ts` source text — see the test's own comment on why that undercounts), which enemy letters some level grid places, and separately records the three `spawnEnemy` call sites outside the loader (`src/enemies/Boss.ts:195,209`, `src/player/Player.ts:161`) that summon an enemy by name — all three always name `"z"` or `"f"`, never anything else. That count and content are re-derived by a guard test, not just hand-copied: it scans `src/` for every occurrence of the literal substring `spawnEnemy(` outside `LevelLoader.ts` and asserts there are exactly three and that every string literal on them is accounted for. Review round 1 proved the gap this closes — retargeting `Boss.ts:209` from `spawnEnemy("f",...)` to `spawnEnemy("q",...)` left all three original tests green, because `q` was already sitting in `UNREACHABLE` and neither `PLACED` nor the hand-written `SUMMONED` set noticed the new call site. Every other `ENEMY_DEFS` letter is placed by some level. `B` (230hp, `slam:true`, a melee brute one tier under the dungeon boss `E`) and `q` (`AFRIT` — `fly`, `range:14`, `orb:"afrit"`, `burst:true`, `deathBoom:true`) are placed in no level grid and summoned nowhere by name, in either `src/` or `reference/sonsurum.html`: the player cannot meet either, ever. Both are recorded in the test's `UNREACHABLE` set with a one-line reason rather than silently ignored, so a future def added and placed nowhere, or `B`/`q` placed without being removed from the set, fails a named test. **This doubles two already-known dead fields, both authored on `q` alone:** `burst` is one of KNOWN-15's ten fields `spawnEnemy` never copies onto a spawned enemy, and `q` is `burst`'s *only* author — so fixing KNOWN-15 for `burst` (making `spawnEnemy` copy it) would still leave it doing nothing, because the only enemy that would ever carry it is never spawned. `deathBoom` is KNOWN-16's still-open field, authored on `q` and read nowhere in `src/`; same doubling — wiring `killEnemy` to consult it would still be inert for the same reason. Both claims (that `q` is `burst`'s and `deathBoom`'s sole author, and that `spawnEnemy`'s literal copies neither) were re-verified directly against `src/enemies/EnemyDefs.ts` and `src/world/LevelLoader.ts` for this entry rather than assumed. **A plausible reason nobody noticed `B` and `q` going unplaced**: `src/world/levels/index.ts:17`'s grid-legend docstring reads `enemies z f g m t w s B · bosses E U Q` — it lists `B` as a placeable enemy glyph (it is placed nowhere), omits `q` entirely, and names three bosses where `ENEMY_DEFS` has eight (`E,U,Q,Z,N,H,V,G`). A legend that advertises `B` as something levels place is exactly the kind of stale comment that would make a missing placement look intentional rather than absent. This docstring is inherited from the frozen reference and already known inaccurate (see KNOWN-4's closing note); it is not fixed here — that correction belongs to the roster phase along with the rest of the legend. The test's second assertion — every glyph a level grid places is claimed by some table (enemy, prop, item, or structural) — is KNOWN-4/KNOWN-11's bug class (a glyph claimed by *two* tables) approached from the other side (claimed by *none*); today it finds nothing, and is worth having anyway because Phase 4 hand-authors new grids for levels 2-4. **`B` and `q` are not fixed here** — placing or summoning either is a roster-composition decision, and the coming cut may delete both outright; that call belongs to a human who has played the game. See `docs/superpowers/specs/2026-09-12-phase3-roster.md`, written from this row's own measurement: §5 names `B`'s and `q`'s successors (The Bellringer and The Suspended respectively) and §8 states the options for how the cut's level-placement fallout is sequenced against Phase 4. | Phase 3 — the roster cut |
| KNOWN-19 | **Closed by Phase 3 Part D's fix round.** `startGame`'s `loadLevel` (`src/world/LevelLoader.ts`) arms timers through `src/core/Timers.ts`'s `after()`, the longest being the 1400ms `after(()=>say("lvl"+idx,true),1400)` on its final line. A test file that drives `startGame`/`loadLevel` — directly, or through a NEW GAME/chapter-select click — while `globalThis.setTimeout` is still the real one (not currently wrapped by that same call's own `tests/support/domStubs.ts`'s `installFakeClock()`) leaves that real timer armed after its last test; whether it fires before or after Vitest tears the file's jsdom environment down is then a race against how long the rest of the suite keeps the worker process alive, and losing it throws `missing element #lvltitle` out of `say()` with no test attached — a red gate caused by nothing that changed. Observed for the first time in Phase 3 Part D, when a new 75-second trace file kept the pool alive long enough for `schedulerWiring.test.ts`'s timer to land; the same full suite passed cleanly on the next run, the signature of a race rather than a regression. **This row originally named "five" files and stated the count as if it were exhaustive — it wasn't, in both directions.** A review of the fix found three more with the identical defect (`contextWiring.test.ts`, `musicCancellationWiring.test.ts`, `timerCancellationWiring.test.ts` — each installs `installFakeClock()` for individual tests, but its own boot's NEW GAME click, and at least one later `loadLevel(...)` call in the latter two, runs outside that window regardless), and found that one of the original five, `renderWidthBootWiring.test.ts`, was never actually affected: it only `await import("../../src/main")`s and never clicks a menu row or calls `loadLevel` at all, so `startGame` — and every `after()` call site downstream of it — never runs there. Do not trust a hand-counted list of affected files, this row's own prior version included; **re-derive it** instead: take every file `grep -l "src/main" tests/integration/*.test.ts` names (today: `schedulerWiring`, `wiring`, `gpuDisposeWiring`, `positionalCallers`, `renderWidthBootWiring`, `contextWiring`, `musicCancellationWiring`, `timerCancellationWiring`, `bossTrace`/`combatTrace`/`trace` via `gameplayTrace.ts`'s own `runTrace`), drop any file that never actually calls `startGame` or `loadLevel` (directly, or via a simulated menu click) — `renderWidthBootWiring.test.ts` and the three `gameplayTrace.ts`-based trace files fall out here, the traces because `runTrace` installs `installFakeClock()` around the *entire* run, including every menu click and `loadLevel`, restored only in its own `finally` — and of what remains, drop only a file whose *every* such call runs while a fake clock installed by that same file is active for its whole duration. Whatever is left needs the fix. All seven files the re-derivation currently names (every one but `renderWidthBootWiring.test.ts`) now carry the same two-line `afterAll(() => { clearAllTimers(); clearScheduled(); })` `schedulerWiring.test.ts` introduced first. If a future test file boots `src/main.ts`, re-run this derivation against it rather than assuming this list, or its count, is still complete. | **closed** — Any plan that touches the test harness should still re-derive this list rather than trust it |
| KNOWN-20 | **STILL OPEN — the mechanism is built and measured, not wired in.** Audio synthesis draws from `Math.random()` at six sites in `src/audio/` — `Sfx.ts:39` (`bang`), `Voice.ts:29/50/67/105` (`noiseBuf`, `growl`, `gurgle`, `snarl`) and `AudioEngine.ts:293` (the ambience LFO); `Sfx.ts:59`'s `boom` is a seventh site that has just never fired in a committed trace script — and `tests/integration/gameplayTrace.ts`'s `runTrace` seeds `Math.random` to make gameplay deterministic. So **every sound that fires consumes the same stream gameplay reads**, and any audio-only change (a cooldown that lets a sound fire for the first time, a resynthesized weapon report, a new sting) can silently move a committed trace fixture, or worse, change how a level plays out. This is the same class of finding as `docs/STATUS.md`'s "The finding that cost the most, and taught the most" (Phase 2 Part A: `THREE.MathUtils.generateUUID()` burning four `Math.random()` calls per object into the identical stream) — fixed there by `installUuidStub`; a reader of one should read the other. **Measured, not estimated, per level** (six-site probe, live, across all three committed traces): **level 0 = 38 draws, level 1 = 137, level 2 = 1041.** The fix already exists — `installAudioStub()` in `tests/integration/gameplayTrace.ts` (committed alongside `installUuidStub`, covered by `tests/integration/audioStub.test.ts`'s 5 cases, verified by live probe to catch exactly those six sites and nothing outside `src/audio/`) — and wiring it in is one line in `runTrace` plus its `restore()` in the same `finally` as the UUID stub's. It is deliberately **not called**. The player-feedback round 1 fix round measured the true cost of wiring it rather than trusting the original estimate ("one more fixture") and found it much higher: removing draws from a shared seeded stream re-indexes every draw after them, and level 2's 1041 removed draws change how the boss fight itself plays out — the Corrupted Priest ends the fight at 942hp against the 594hp (33%) phase-3 threshold, never reaching phase 3, reddening three *behavioral* `bossTrace.test.ts` assertions (the phase-3 name-bar transition, `hp<maxhp*.33`, and Phase 3 Part D's form-swap structural guard) that no fixture regeneration can fix, because they assert what the run *does*, not what it recorded. So wiring the stub in costs **three regenerated fixtures plus a retuned boss script**, not the one fixture originally budgeted — real work, with real risk to a characterization recording two plans in the making, and out of scope for a round about player-reported bugs. **The cost of leaving it unwired**: every future audio change keeps moving fixtures (`trace-level0.json` moved twice this round already, from the kick cooldown and the sprint camera bob — see that file's own header for the frame-by-frame split), and this same plan's Task 4 changes the weapon report synthesis in `src/audio/Sfx.ts`, which will need the same treatment. **A correction worth keeping with the number**: an early measurement in this round claimed `bang()`'s noise-buffer fill draws roughly 1100 values (true in a real browser, at its sample rate); in this harness, `tests/support/domStubs.ts`'s `createBuffer()` returns `{ getChannelData: () => new Float32Array(1) }` regardless of the requested length, so every noise fill in a trace is **exactly one** draw here. The mechanism is identical; only the magnitude differs, and a future reader sizing the problem needs the harness number, not the browser one. **Update, player feedback round 1 task 4 (2026-09-17): the weapon-report rebuild this row predicted "will need the same treatment" moved no fixture, because it was designed not to.** `src/audio/Sfx.ts`'s new `gunshot()` fills **one** noise buffer — the crack and the body are the same noise through two filters, not two independent noises — which is exactly the one `Math.random()` fill loop the single `bang()` it replaces made. Per weapon, in harness draws: pistol 1→1, rifle 1→1, tommy gun 1→1, sniper 1→1, nail cannon 1→1, and **shotgun 2→1** (its old closure was two `bang`s). All three committed traces stayed green, which is the evidence: a changed draw count re-indexes the shared seeded stream and could not have left them untouched. **The shotgun's 2→1 is real and merely unobserved** — `trace.test.ts` and `combatTrace.test.ts` fire the default slot 0 (pistol) and `bossTrace.test.ts` sets `S.cur=6` (nail cannon), so no committed trace fires slot 1 at all. A future trace script that does will re-index from its first shotgun shot, and so will the `.ogg` swap of Phase 1 Task 6, which removes the draw entirely (see `docs/assets.md`). The row stays open: this task avoided the cost for one change, it did not remove the coupling. | Whichever plan takes on isolating audio from the gameplay stream |
| KNOWN-21 | **Three doc comments in `src/audio/AudioEngine.ts` state the opposite of what its code does, and the code is the correct side.** The module doc comment, `emitAt()`'s doc comment and `emitHere()`'s doc comment all describe a **consume-on-first-use** contract for `pendingPos` — "`masterBus()`/`echoBus()` already clear `pendingPos` themselves once they consume it (see `busFor()`)", "Consumes (clears) `pendingPos` as soon as it reads it, so the position affects only this one call", and the module header's "for those, only the first sub-call would be positioned under this contract." `busFor()` does no such thing. Its body says so in as many words — "Deliberately NOT cleared here" — with a good reason (`snarl` calls two emitters on six of eleven branches, and clearing would leave half an enemy bark inside the player's head), and `tests/audio/positional.test.ts` already pins the real behavior by name: "every accessor call after one `emitAt()` gets a panner, not just the first." So the comments are stale, not the code; they appear to predate a deliberate change to `busFor()` that did not sweep them. **Consequence today is zero and the trap is real**: the only live positional form anywhere in `src/` is `at()` (grep confirms no bare `emitAt()` call site outside `AudioEngine.ts` itself), so nothing depends on the wrong description — but player-feedback round 1 task 4 read those comments, believed them, wrote a test asserting a correctness property that does not exist, and only found out because the test went red. The genuine cost of a multi-accessor emitter is **one extra HRTF panner allocated per bus call** (that same file calls HRTF the expensive node here), not a layer dropped out of the pan. Not fixed in that task: it is a comment-only change in a module outside a round scoped to one reported bug, and it deserves a deliberate sweep of all three sites at once rather than a drive-by. `src/audio/Sfx.ts`'s `gunshot()` doc comment and `tests/behavior/weaponReport.test.ts` both carry the correction inline so the next reader hits it from either direction. | Any plan that next touches `src/audio/AudioEngine.ts` |
